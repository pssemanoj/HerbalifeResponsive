#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-hash/bin/grunt-hash" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-hash/bin/grunt-hash" "$@"
  ret=$?
fi
exit $ret
